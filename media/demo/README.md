# Playwright demo

The Playwright Media workflow records the real app and generates:

- `pot-of-mannah-demo.webm`
- `pot-of-mannah-demo.mp4` when `ffmpeg` is available

The recording uses a disposable demo database created by `scripts/seed_demo.py`.
